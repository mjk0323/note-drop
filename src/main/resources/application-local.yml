spring:
  datasource:
    master:
      url: jdbc:mysql://localhost:3306/notedrop?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
      hikari:
        maximum-pool-size: 5
        connection-timeout: 3000
        max-lifetime: 1800000
        leak-detection-threshold: 5000
    replica:
      url: jdbc:mysql://localhost:3306/notedrop?serverTimezone=UTC&characterEncoding=UTF-8
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
      hikari:
        maximum-pool-size: 5
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: true

jwt:
  secret: localdevlocalsecretkey32byteslong!!
